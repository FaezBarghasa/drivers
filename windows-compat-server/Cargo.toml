[package]
name = "windows-compat-server"
version = "0.1.0"
edition = "2024"
description = "Windows Application Compatibility (WAC) Server for Redox OS"
authors = ["Redox OS Developers"]
license = "MIT"

[dependencies]
redox_syscall = "0.4"
redox-scheme = "0.2"
redox_daemon = "0.1"
spin = "0.9"
bitflags = "2"
sled = "0.34"
serde = "1.0"
serde_derive = "1.0"
bincode = "1.3"

[features]
default = []
